---
import LinkBullet from '@components/LinkBullet.astro';

interface Props {
	pagePath: string;
}

const { pagePath } = Astro.props;
---

<footer id="footer" class="l-footer">
	<div class="l-footer__nav">
		<ul class="p-footer-nav">
			{
				[
					{ path: '/info', name: '制作者' },
					{ path: '/privacy', name: 'プライバシーポリシー' },
					{ path: '/technology', name: '当サイトの Web 技術' },
					{ path: '/contact', name: '問い合わせ' },
				].map((page) => (
					<li>
						{page.path === pagePath && <LinkBullet>{page.name}</LinkBullet>}
						{page.path !== pagePath && <LinkBullet href={page.path}>{page.name}</LinkBullet>}
					</li>
				))
			}
		</ul>
	</div>

	{
		!['/contact'].includes(pagePath) && !pagePath.startsWith('/admin/') && (
			<div class="l-footer__ads">
				<section class="p-footer-ads">
					<h2 class="p-footer-ads__hdg">広告</h2>

					<ins class="adsbygoogle js-ads-google" data-ad-layout="in-article" data-ad-format="fluid" data-ad-client="ca-pub-3297715785193216" data-ad-slot="9399879773" />
				</section>
			</div>
		)
	}
</footer>
