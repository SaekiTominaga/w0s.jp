<dl class="list">
	<slot />
</dl>

<style>
	@layer component {
		.list {
			& > :global(div + div) {
				margin-block-start: 0.5em;
			}

			& > :global(div > dt) {
				font-weight: var(--font-weight-bold);
			}

			& > :global(div > dd) {
				margin-inline-start: 1em;
			}
		}
	}
</style>
