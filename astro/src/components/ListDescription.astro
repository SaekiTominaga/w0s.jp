<dl class="list">
	<slot />
</dl>

<style>
	@layer component {
		.list {
			& > :global(dt) {
				font-weight: var(--font-weight-bold);
			}

			& > :global(dd + dt) {
				margin-block-start: 0.5em;
			}

			& > :global(dd) {
				margin-inline-start: 1em;
			}
		}
	}
</style>
