<ul class="kumeta-top-news">
	<slot />
</ul>

<style>
	@layer component {
		.kumeta-top-news {
			display: block flex;
			flex-wrap: wrap;
			gap: 32px;
			line-height: var(--line-height-narrow);
			font-size: calc(100% / pow(var(--font-ratio), 2));
		}
	}
</style>
