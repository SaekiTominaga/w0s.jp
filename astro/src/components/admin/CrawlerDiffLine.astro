---
interface Props {
	type?: 'added' | 'removed';
}

const { type } = Astro.props;
---

<tr class:list={['line', type !== undefined ? `-${type}` : undefined]}>
	<slot />
</tr>

<style>
	@layer component {
		.line {
			--_bg-color: var(--color-white);

			background-color: var(--_bg-color);
			color: var(--color-black);
			font-size: calc(100% / pow(var(--font-ratio), 2));

			&.-added {
				--_bg-color: oklch(from var(--color-green) 100% calc(c - 0.1) h);
			}

			&.-removed {
				--_bg-color: oklch(from var(--color-red) calc(l + 0.3) calc(c - 0.2) h);
			}

			& :global(td) {
				padding: 0.25em 0.5em;
			}

			& :global(td:first-child) {
				text-align: center;
				font-weight: var(--font-weight-bold);
			}
		}
	}
</style>
