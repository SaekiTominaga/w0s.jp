<ol class="step">
	<slot />
</ol>

<style>
	@layer component {
		.step {
			display: block flex;
			flex-wrap: wrap;
			contain: content;
			line-height: var(--line-height-narrow);

			& + :global(*) {
				margin-block-start: calc(var(--stack-margin-base) * 3);
			}
		}
	}
</style>
