import { contact } from './contact.ts';
import { crawlerNews } from './crawlerNews.ts';
import { crawlerResource } from './crawlerResource.ts';

export const server = {
	contact: contact,
	crawlernews: crawlerNews,
	crawlerresource: crawlerResource,
};
